# ICT AutoTrader - Development Workflow

## 서브태스크 완료 규칙

각 서브태스크 완료 시 **반드시** 다음 2가지를 수행해야 합니다:

### 1. Git Commit & Push ✅

```bash
# 구현 완료 후
git add .
git commit -m "feat(task-X.Y): [서브태스크 제목]

- 구현 내용 요약
- 주요 변경사항

Refs: Task X.Y"

git push origin main
```

**커밋 메시지 형식:**
- `feat(task-X.Y):` - 새로운 기능 구현
- `test(task-X.Y):` - 테스트 추가/수정
- `docs(task-X.Y):` - 문서 작성/수정
- `fix(task-X.Y):` - 버그 수정
- `refactor(task-X.Y):` - 리팩토링

### 2. Documentation 생성/업데이트 📚

서브태스크 완료 후 관련 문서 업데이트:

**업데이트할 문서:**
- `docs/architecture.md` - 새로운 컴포넌트 추가 시
- `docs/testing.md` - 테스트 전략 추가 시
- `README.md` - 사용법 변경 시
- 코드 내 docstring - 모든 public 함수/클래스

**문서 작성 원칙:**
- 구현한 내용의 목적과 사용법 명확히 기술
- 코드 예제 포함
- 의존성 및 전제조건 명시
- 주요 함수/클래스에는 반드시 docstring 작성

## 완료 체크리스트

각 서브태스크 완료 전 확인:

- [ ] 코드 구현 완료
- [ ] 테스트 작성 및 통과 (`pytest tests/`)
- [ ] Docstring 작성 완료
- [ ] Git commit & push 완료
- [ ] 관련 문서 업데이트 완료
- [ ] `task-master set-status --id=X.Y --status=done` 실행

## 주의사항 ⚠️

**절대 건너뛰지 말 것:**
1. ❌ 테스트 없이 commit 금지
2. ❌ 문서 없이 서브태스크 완료 금지
3. ❌ 각 서브태스크는 독립적으로 commit (한꺼번에 여러 서브태스크 commit 금지)

## 예시: Task 1.1 완료 시

```bash
# 1. 구현 완료
mkdir -p src/core tests/unit docs logs
touch src/__init__.py src/core/__init__.py ...

# 2. 테스트 작성 및 실행
# tests/test_structure.py 작성
pytest tests/test_structure.py

# 3. 문서 작성
# docs/architecture.md 업데이트

# 4. Commit & Push
git add .
git commit -m "feat(task-1.1): Create complete directory structure

- Created src/ with all subdirectories (core, data, strategy, execution, notification)
- Created tests/ with unit, integration, fixtures
- Created docs/ with architecture.md and testing.md
- Created logs/ directory
- Added all __init__.py files for Python packages

Refs: Task 1.1"

git push origin main

# 5. Task Master 상태 업데이트
task-master set-status --id=1.1 --status=done
```

## 개발 흐름 (Development Flow)

```
┌─────────────────────────────────────────────────────────┐
│ 1. task-master show X.Y (서브태스크 확인)                 │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 2. task-master set-status --id=X.Y --status=in-progress │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 3. 코드 구현 (src/)                                      │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 4. 테스트 작성 (tests/)                                  │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 5. pytest tests/ (테스트 실행 및 통과 확인)               │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 6. 문서 업데이트 (docs/, docstrings)                     │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 7. git commit & push                                    │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 8. task-master set-status --id=X.Y --status=done       │
└─────────────────────────────────────────────────────────┘
```

---

**생성일:** 2025-11-30
**마지막 업데이트:** 2025-11-30
